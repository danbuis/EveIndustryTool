@page "/"
@inject Data.StaticData StaticData

<PageTitle>Index</PageTitle>

<h1>Hello, world!</h1>

@foreach (int typeID in StaticData.Blueprints.Keys)
{
    <li>
        <a href="/blueprint/@StaticData.MapIntToItemName(typeID)">@StaticData.MapIntToItemName(typeID)</a>
    </li>
}
